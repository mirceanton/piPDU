---
.flake8:
  stage: lint
  image: python:3.9

  variables:
    PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

  cache:
    paths:
      - .cache/pip
      - venv/

  before_script:
    - python --version
    - pip install flake8

  script:
    - cd $CI_SERVICE_REPO
    - flake8 ./src/*
